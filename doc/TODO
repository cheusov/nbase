* indent, renice, ul, mtree -> netbsd sources

* fails to detect vdprintf on NetBSD
* wgetwln, reallocarray, fopencookie on NetBSD

* Disable modules based on configure tests
* Convert imp_macro.h to mk/* module
* Support for all POSIX utils
* warnings on Darwin:
  - stty
