* Bug #9: at+atd bc(bsd) cap_mkdb cu+tip? ftp
lndir(x11) newsyslog rdate realpath(HEAD)
tftp spell su users who

* Add man, mandoc
* Add crond, crontab

* sleep on Darwin, OpenBSD
* kill on Darwin, OpenBSD

* rename compatlib to libnbase
* indent, renice, ul, mtree -> netbsd sources

* fails to detect vdprintf on NetBSD
* wgetwln, reallocarray, fopencookie on NetBSD

* Disable modules based on configure tests
* Convert imp_macro.h to mk/* module
* Support for all POSIX utils
* warnings on Darwin:
  - stty
